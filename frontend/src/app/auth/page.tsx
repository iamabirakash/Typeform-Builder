"use client";

import Link from "next/link";
import { useEffect, useState } from "react";
import { useRouter } from "next/navigation";
import { API_URL, api, saveToken } from "@/lib/api";

export default function AuthPage() {
  const router = useRouter();
  const [mode, setMode] = useState<"login" | "signup">("login");
  const [name, setName] = useState(""); const [email, setEmail] = useState(""); const [password, setPassword] = useState(""); const [error, setError] = useState(""); const [loading, setLoading] = useState(false);
  useEffect(() => { const params = new URLSearchParams(window.location.search); const token = params.get("token"); if (params.get("mode") === "signup") setMode("signup"); if (token) { saveToken(token); router.replace(params.get("next") || "/forms"); } }, [router]);
  async function submit(event: React.FormEvent) { event.preventDefault(); setLoading(true); setError(""); try { const result = await api<{ access_token: string }>(`/api/auth/${mode === "login" ? "login" : "signup"}`, { method: "POST", body: JSON.stringify(mode === "login" ? { email, password } : { name, email, password }) }); saveToken(result.access_token); router.push("/forms"); } catch (err) { setError(err instanceof Error ? err.message : "Authentication failed"); } finally { setLoading(false); } }
  return <main className="grid min-h-screen bg-[#f7f6f2] lg:grid-cols-2"><section className="hidden bg-[#171719] p-12 text-white lg:flex lg:flex-col lg:justify-between"><Link href="/" className="text-2xl font-black tracking-[-0.08em]">typeform<span className="text-[#d9ff4f]">.</span></Link><div><p className="text-sm font-bold uppercase tracking-[0.2em] text-[#d9ff4f]">A better way to ask</p><h1 className="mt-6 max-w-lg text-7xl font-black leading-[0.88] tracking-[-0.08em]">Make space for good questions.</h1><p className="mt-8 max-w-md text-lg leading-8 text-white/55">Build forms that feel like a conversation and keep every answer in one thoughtful workspace.</p></div><p className="text-sm text-white/35">Your workspace, your forms, your signal.</p></section><section className="flex items-center justify-center px-6 py-12"><div className="w-full max-w-md"><Link href="/" className="text-2xl font-black tracking-[-0.08em] lg:hidden">typeform<span className="text-[#635bff]">.</span></Link><div className="mt-12 rounded-[2rem] bg-white p-7 shadow-xl sm:p-10"><p className="text-sm font-bold uppercase tracking-[0.18em] text-[#635bff]">Welcome</p><h2 className="mt-3 text-3xl font-black tracking-tight">{mode === "login" ? "Log in to your workspace" : "Create your workspace"}</h2><button onClick={() => { window.location.href = `${API_URL}/api/auth/google/start`; }} className="mt-8 flex w-full items-center justify-center gap-3 rounded-xl border border-slate-200 px-4 py-3 text-sm font-semibold hover:bg-slate-50"><span className="grid h-6 w-6 place-items-center rounded-full font-bold shadow-sm">G</span>Continue with Google</button><div className="my-6 flex items-center gap-3 text-xs text-slate-400"><span className="h-px flex-1 bg-slate-200" />or email<span className="h-px flex-1 bg-slate-200" /></div><form onSubmit={submit} className="space-y-4">{mode === "signup" && <label className="block text-sm font-semibold">Name<input required value={name} onChange={(e) => setName(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 bg-white px-4 py-3 font-normal outline-none focus:border-[#635bff]" /></label>}<label className="block text-sm font-semibold">Email<input required type="email" value={email} onChange={(e) => setEmail(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 bg-white px-4 py-3 font-normal outline-none focus:border-[#635bff]" /></label><label className="block text-sm font-semibold">Password<input required minLength={8} type="password" value={password} onChange={(e) => setPassword(e.target.value)} className="mt-2 w-full rounded-xl border border-slate-200 bg-white px-4 py-3 font-normal outline-none focus:border-[#635bff]" /></label>{error && <p role="alert" className="rounded-xl bg-red-50 px-4 py-3 text-sm text-red-700">{error}</p>}<button disabled={loading} className="w-full rounded-xl bg-[#635bff] px-4 py-3.5 text-sm font-bold text-white hover:bg-[#5148e8] disabled:opacity-60">{loading ? "Please wait..." : mode === "login" ? "Log in" : "Create account"}</button></form><p className="mt-7 text-center text-sm text-slate-500">{mode === "login" ? "New here?" : "Already have an account?"} <button onClick={() => { setMode(mode === "login" ? "signup" : "login"); setError(""); }} className="font-bold text-[#635bff]">{mode === "login" ? "Create an account" : "Log in"}</button></p></div></div></section></main>;
}

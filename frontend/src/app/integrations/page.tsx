import type { Metadata } from "next";
import { ComingSoonPage } from "@/components/coming-soon-page";

export const metadata: Metadata = { title: "Connect | Typeform Clone" };
export default function IntegrationsPage() { return <ComingSoonPage title="Connect" description="Connect forms to your favorite tools, webhooks, and automated workflows." />; }

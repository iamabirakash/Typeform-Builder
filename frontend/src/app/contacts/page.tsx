import type { Metadata } from "next";
import { ComingSoonPage } from "@/components/coming-soon-page";

export const metadata: Metadata = { title: "Contacts | Typeform Clone" };
export default function ContactsPage() { return <ComingSoonPage title="Contacts" description="Bring your audience, responses, and contact records together in one organized workspace." />; }

import type { Metadata } from "next";
import { ComingSoonPage } from "@/components/coming-soon-page";

export const metadata: Metadata = { title: "Insights | Typeform Clone" };
export default function InsightsPage() { return <ComingSoonPage title="Insights" description="Turn your form responses into clear trends, useful metrics, and decisions your team can act on." />; }
